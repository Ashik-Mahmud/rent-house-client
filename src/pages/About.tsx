import DuelSlider from "../components/DuelSlider";

type Props = {};

const About = (props: Props) => {
  return (
    <div>
      About
      <DuelSlider />
    </div>
  );
};

export default About;
